{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{'nursery.list.name'|trans}}</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="{{'total.edit'|trans}}" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('homepage')}}">{{'total.back'|trans}}</a>
        </li>
       
    </ul>
{% endblock %}
